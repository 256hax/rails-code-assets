<%= form_with(model: inboxes_spam, local: true) do |form| %>
  <% if inboxes_spam.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(inboxes_spam.errors.count, "error") %> prohibited this inboxes_spam from being saved:</h2>

      <ul>
      <% inboxes_spam.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :spam_flag %>
    <%= form.check_box :spam_flag %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
